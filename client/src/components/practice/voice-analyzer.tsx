import { useEffect } from "react";

export function VoiceAnalyzer() {
  // This component is used for background audio processing
  // It doesn't render any UI but handles the audio analysis logic
  
  useEffect(() => {
    // Audio processing logic would go here
    // This is handled by the useVoiceAnalyzer hook
  }, []);

  return null;
}
